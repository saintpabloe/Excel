Webpack (imports/exports)
    Babel
    Scss (предпроцессор для CSS)
    Eslint (следит за чистотой коды)
    Two modes
        - Dev (dev server - инструмент, который позволяет перезагружать автоматически окно браузера при изменение кода,
         source map - исходные карты, которые будут добавлятсья для SCSS, указывать на те моддули, где велась разработка,
         Eslint - чистый код, содержание кода в единственном стиле
         not minified JS & CSS - не минифицируем код, чтобы понимать как выглядит код и что происходит в нем; минификация
         может занимать какое то время. в продакшн моде мы минимицируем)

        - Prod (minified all)
Git (позволяет следить за версионностью вашего приложения, некий бэкап, система контроля версий)

    Production
    1.0.0
    0 ------
    Development
    1.0.2
    1 --------  Pool request (добавляется новая фича)
    2 --------- | (пытается совместить свою версию с новой фичей со основным дев проектом, при успешном внетрении
    данная ветка удаляется)